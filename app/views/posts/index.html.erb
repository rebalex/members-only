<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <strong><%= link_to post.title, "/posts/#{post.id}" %></strong>
    </li>
      <% if signed_in? %>
        Posted by:
        <%= post.user.username %>
      <% end %>
      <br>
      <br>
  <% end %>
</ul>
